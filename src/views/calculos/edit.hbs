<div class="container p-4">
    <div class="row">
        <div class="col-md-4 mx-auto">

    <form class="container" id="container" name="calculator" action="/edit/{{historial.id}}" method="POST">
        <div class="form-group">
            <h3>Calculadora</h3>
            <p>Haz algun calculo para ver el resultado.</p>
            <input type="textfield" name="calculo" value="{{historial.calculo}}"  class="form-control"> <br>
            <input type="textfield" name="resultado" value="{{historial.resultado}}"  class="form-control"> <br>
            <button type="button" class="btn btn-primary" onClick="document.calculator.resultado.value=eval(document.calculator.calculo.value)"> = </button>
            <button type="sumbit" class="btn btn-primary"> SAVE </button>
        </div>
    </form>






    <form class="container" id="container" name="calculator" action="/" method="POST">
        <div class="form-group">
            <h3>Calculadora</h3>
            <p>Haz algun Cálculo para ver el resultado.</p>
            <input type="textfield" name="calculo" placeholder="Cálculo" class="form-control"> <br>
            <input type="textfield" name="resultado" placeholder="Resultado" class="form-control"> <br>
            <button type="button" class="btn btn-primary" onClick="document.calculator.resultado.value=eval(document.calculator.calculo.value)"> = </button>
            <button type="sumbit" class="btn btn-primary"> Guardar </button>
        </div>
    </form>





<div class="container p-4">
    <div class="row">
            <table class="table table-bordered">
                <thead>
                    <tr>
                    <th scope="col">Cálculo</th>
                    <th scope="col">Resultado</th>
                    <th scope="col">Acción </th>
                    </tr>
                </thead>
                <tbody>
                    {{#each historial}}
                        <tr>
                            <td>{{calculo}}</td>
                            <td>{{resultado}}</td>
                        
                        <td>
                            <a href="/edit/{{id}}" class="btn btn-primary">Recuperar</a>
                            <a href="/delete/{{id}}" class="btn btn-danger">Eliminar</a>
                        </td>
                    </tr>
                    {{/each}}
                </tbody>
            </table>
    </div> 
</div>